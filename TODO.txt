
* Graphs seem to show motion about 5 seconds after it happens.
  Maybe in Grapher.read_stats_if_needed, it can try to align the numbers based on the start_time in the .json file

* Auto-reload config when file is changed. Things will be notified with old and new config.
* Re-generate (delete) images if it detects change to image settings.


* Put UI controls on live view, have '/controls' (def adjust_camera_controls) endpoint to adjust in real time